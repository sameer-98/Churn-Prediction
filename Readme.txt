- cd to the directory of the folder where the notebook, data and requirements.txt file are placed
- Create environment in the preferred directory using
	python -m venv project 
- Activate the environment using
	\project\Scripts\activate.bat
- install the dependencies using
	pip install -r requirements.txt
- The notebook should now work